{
  "name": "slimmage-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter 'slimmage-core' test",
    "test:e2e": "cd packages/slimmage-test && bun run test"
  }
}
